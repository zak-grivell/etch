[package]
name = "cli"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "etch"
path = "src/main.rs"

[dependencies]
clap = { version = "4.6.4", features = ["derive"] }
parser = { path = "../parser" }
evaluator = { path = "../evaluator" }
